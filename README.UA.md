# Neovim IDE - Професійне середовище розробки з фокусом на таби

## Огляд

Ця конфігурація Neovim створює сучасне IDE з акцентом на **роботу з табами** замість традиційного управління буферами. Такий підхід робить його схожим на сучасні IDE, такі як VSCode, IntelliJ IDEA або Sublime Text, де кожен файл відкривається в своєму табі для легшої навігації та організації.

## Ключова філософія: Робота з табами

На відміну від традиційних Vim-процесів, які сильно покладаються на буфери, ця конфігурація надає пріоритет табам:

- **Файли відкриваються в табах** - кожен файл автоматично отримує свій таб
- **LSP навігація відкривається в табах** - переходи до визначень, посилань тощо відкриваються в нових табах
- **Telescope відкривається в табах** - результати пошуку файлів відкриваються в табах
- **Розумне управління табами** - сиріт буфери автоматично очищаються
- **Візуальні індикатори табів** - чітка панель табів показує всі відкриті файли

## Основні можливості

- **Language Server Protocol (LSP)** - повні IDE функції для багатьох мов
- **Централізована система хоткеїв** - всі команди через Legendary з українською розкладкою
- **Розумний файловий провідник** - модальне дерево файлів з інтеграцією табів
- **Розширений пошук** - знаходження файлів і тексту з результатами в табах
- **Перемикач тем** - кілька красивих тем з попереднім переглядом
- **Інтеграція терміналу** - вбудований термінал з розумінням проектів
- **Інтеграція Git** - візуальний статус git та операції
- **Мінімальні автозапуски** - лінтери та форматери запускаються вручну
- **Система хоткеїв за групами** - логічна організація команд

## Встановлення

### 1. Встановлення залежностей та інструментів

Оновлення системи та встановлення основних інструментів.

```bash
sudo apt remove -y npm;
sudo apt autoremove -y;

sudo apt update && \
sudo apt install -y \
  nodejs \
  npm \
  build-essential \
  cmake \
  gettext \
  ninja-build \
  unzip \
  curl \
  git \
  libtool \
  libtool-bin \
  autoconf \
  automake \
  pkg-config \
  clangd \
  golang-go \
  fd-find \
  luarocks \
  cargo \
  composer \
  xclip \
  python3-pip \
  ripgrep \
  codespell \
  libssl-dev \
  libfreetype6-dev \
  libfontconfig1-dev \
  libxcb-shape0-dev \
  libxcb-xfixes0-dev \
  libxkbcommon-dev \
  libxkbcommon-x11-dev \
  libegl1-mesa-dev
```

### 2. Встановлення Nerd Fonts

Завантаження та встановлення популярних Nerd Fonts.

```bash
adwaita="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/AdwaitaMono.zip"
anonymous="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/AnonymousPro.zip"
jetbrains="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/JetBrainsMono.zip"
firacode="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/FiraCode.zip"
cascadia="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/CascadiaCode.zip"
hack="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/Hack.zip"
sourcecode="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/SourceCodePro.zip"
ubuntu="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/UbuntuMono.zip"
dejavu="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/DejaVuSansMono.zip"
inconsolata="https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/Inconsolata.zip"

mkdir -p ~/.local/share/fonts && \
mkdir -p ~/tmp/fonts && \
cd ~/tmp/fonts && \
wget $adwaita && \
wget $anonymous && \
wget $jetbrains && \
wget $firacode && \
wget $cascadia && \
wget $hack && \
wget $sourcecode && \
wget $ubuntu && \
wget $dejavu && \
wget $inconsolata && \
unzip -o "*.zip" -d ~/.local/share/fonts/ && \
rm *.zip && \
fc-cache -fv
```

**⚠️ ВАЖЛИВО**: Встановіть один з шрифтів `UbuntuMono Nerd Font Mono` як шрифт за замовчуванням у вашому терміналі для правильного відображення іконок.

### 3. Встановлення Neovim

Видалення старої версії.

```bash
sudo apt remove neovim
sudo rm -f /usr/bin/nvim
sudo rm -f /usr/local/bin/nvim
sudo rm -rf /usr/local/share/nvim/
sudo rm -rf /opt/nvim-linux-x86_64
sudo rm -rf /opt/nvim-linux-arm64
sudo rm -rf /opt/nvim
```

Очищення кешу.

```bash
rm -rf ~/.local/share/nvim/
rm -rf ~/.local/state/nvim/
```

Тимчасова директорія.

```bash
mkdir -p /tmp/nvim
cd /tmp/nvim
```

Встановлення для x86_64.

```bash
curl -LO https://github.com/neovim/neovim/releases/download/v0.11.5/nvim-linux-x86_64.tar.gz
tar xzvf nvim-linux-x86_64.tar.gz
sudo mv nvim-linux-x86_64 /opt/nvim
sudo ln -sf /opt/nvim/bin/nvim /usr/bin/nvim
hash -r
```

Встановлення для arm64.

```bash
curl -LO https://github.com/neovim/neovim/releases/download/v0.11.5/nvim-linux-arm64.tar.gz
tar xzvf nvim-linux-arm64.tar.gz
sudo mv nvim-linux-arm64 /opt/nvim
sudo ln -sf /opt/nvim/bin/nvim /usr/bin/nvim
hash -r
```

Перевірка версії.

```bash
nvim --version | head -n 5
```

```txt
NVIM v0.11.5
Build type: Release
LuaJIT 2.1.1741730670
Run "nvim -V1 -v" for more info
```

### 4. Встановлення Python інструментів

Глобальна установка Python форматерів.

```bash
pip3 install black isort mypy
```

Або для конкретного проекту з віртуальним середовищем.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install black isort django mypy # додайте інші залежності за потребою
```

### 5. Встановлення конфігурації Neovim IDE

Очищення кешу та клонування репозиторію.

```bash
bash -lc '
set -euo pipefail
cd "$HOME"
config_dir="${XDG_CONFIG_HOME:-$HOME/.config}/nvim"
data_dir="${XDG_DATA_HOME:-$HOME/.local/share}/nvim"
state_dir="${XDG_STATE_HOME:-$HOME/.local/state}/nvim"
cache_dir="${XDG_CACHE_HOME:-$HOME/.cache}/nvim"
[ -e "$config_dir" ] && mv "$config_dir" "$config_dir.bak.$(date +%s)"
[ -e "$data_dir" ] && mv "$data_dir" "$data_dir.bak.$(date +%s)"
[ -e "$state_dir" ] && mv "$state_dir" "$state_dir.bak.$(date +%s)"
[ -e "$cache_dir" ] && mv "$cache_dir" "$cache_dir.bak.$(date +%s)"
mkdir -p "$config_dir"
mkdir -p "$data_dir"
mkdir -p "$state_dir"
mkdir -p "$cache_dir"
echo -e "\n\nCloning Neovim IDE..."
git clone --depth 1 https://github.com/valsorym/nvim-ide "$config_dir"
# rm -rf "$config_dir/.git"
nvim --headless "+Lazy! sync" "+qa"
echo -e "\n\nNeovim IDE встановлено успішно: $config_dir"
echo "Плагіни будуть встановлені автоматично при першому запуску..."
' && nvim
```

При першому запуску Neovim автоматично:
- Встановить всі плагіни через Lazy.nvim
- Завантажить LSP сервери через Mason
- Налаштує Treesitter парсери

### 6. Vim -> NVim

Встановлення NVim як стандартного редактора.

```bash
sudo update-alternatives --install /usr/bin/vim vim /usr/local/bin/nvim 60
sudo update-alternatives --install /usr/bin/vi vi /usr/local/bin/nvim 60
sudo update-alternatives --install /usr/bin/editor editor /usr/local/bin/nvim 60
```

## Система хоткеїв та команд

### Централізована система команд

Вся конфігурація використовує **Legendary** для централізованого управління командами з підтримкою української розкладки.

#### Основне меню команд
```bash
<Space>               # Головне меню (або <leader>)
<leader><leader>      # Пошук усіх команд
<leader>?             # Перегляд за групами
<leader>:             # Пошук команд
```

#### Групи команд (`<leader>` + літера)
- **`<leader>t`** - 📑 **Tabs** (управління табами)
- **`<leader>w`** - 🏢 **Workspaces** (сесії та робочі простори)
- **`<leader>f`** - 🔍 **Find/Search** (пошук і заміна)
- **`<leader>e`** - 📁 **Explorer** (файли і буфери)
- **`<leader>c`** - 💻 **Code/LSP** (код і LSP операції)
- **`<leader>x`** - ⚙️ **System** (система і налаштування)
- **`<leader>g`** - 🔀 **Git** (Git операції)
- **`<leader>y`** - 📋 **Yank** (буфер обміну)
- **`<leader>u`** - 🎨 **UI/Themes** (інтерфейс і теми)
- **`<leader>d`** - 📝 **Document** (форматування)
- **`<leader>a`** - 🗺️ **Aerial** (навігація по коду)
- **`<leader>s`** - 🔎 **Search** (розширений пошук)

### Функціональні клавіші
```bash
F2    # Зберегти і форматувати
F5    # Попередній таб
F6    # Наступний таб
F7    # Code Inspector (символи документу)
F8    # Список табів
F9    # Файловий провідник
F10   # Список буферів
```

### Навігація табами
```bash
Alt+Left/Right        # Попередній/наступний таб
Alt+1..9             # Перейти до табу 1-9
Alt+h/l              # Перемістити таб вліво/вправо
Ctrl+t               # Новий таб
```

### Основні хоткеї навігації
```bash
gd                   # Перейти до визначення (в новому табі)
gD                   # Перейти до декларації
gi                   # Перейти до імплементації
gr                   # Показати посилання
K                    # Документація під курсором
Ctrl+k               # Сигнатура функції
```

## Мовна підтримка

### 🐍 Python Development

#### Віртуальні середовища
```bash
<leader>cva          # Активувати venv
<leader>cvd          # Деактивувати venv
<leader>cvs          # Статус venv
<leader>cvf          # Знайти venv
<leader>cvc          # Вибрати venv
```

#### Форматування коду
```bash
<leader>df           # Комплексне форматування (isort + black)
<leader>ci           # Сортування імпортів (isort)
<leader>cb           # Форматування Black
F2                   # Збереження + форматування
```

#### Python терміналы
```bash
<leader>xtp          # Python REPL з активним venv
<leader>xtd          # Django shell (якщо є manage.py)
```

#### Діагностика та інструменти
```bash
<leader>ca           # Code Actions
<leader>cr           # Перейменувати символ
<leader>cc           # Діагностика рядка
<leader>cC           # Діагностика всього проекту
```

#### Python налаштування
```bash
:PythonToolsStatus   # Статус Python інструментів
:CreatePyprojectToml # Створити pyproject.toml
:CreatePyrightConfig # Створити pyrightconfig.json
```

#### Лінтери (запуск вручну)
```bash
<leader>cks          # Статус Python інструментів
<leader>ckp          # Створити pyproject.toml
<leader>ckr          # Створити pyrightconfig.json
```

### 🚀 Go Development

#### Форматування і організація
```bash
<leader>df           # Форматування з goimports
F2                   # Збереження + автоформатування
```

#### Go специфічні команди
```bash
gd                   # Перейти до визначення
gi                   # Перейти до інтерфейсу
<leader>ca           # Code Actions (додати імпорти, etc.)
```

#### Go інструменти
```bash
:GoMod tidy          # Очистити go.mod (через LSP)
```

### 🌐 HTML/CSS Development

#### Підтримка шаблонів
- **HTML**: Повна підтримка HTML5 з Emmet
- **Django Templates**: Автоматичне визначення `.html` файлів як Django шаблони
- **CSS/SCSS**: Повна підтримка з автодоповненням

#### Форматування
```bash
<leader>df           # Форматування через Prettier
F2                   # Збереження + форматування
```

#### Emmet розширення
```bash
# В HTML файлах
div.container>ul>li*3    # Розгортає в HTML структуру
Ctrl+y,              # Активувати Emmet (якщо налаштовано)
```

#### Рендеринг
```bash
<leader>dr           # Перемикання рендерингу (Markdown/RST)
```

### 🔧 JavaScript/TypeScript

#### Форматування і лінтинг
```bash
<leader>df           # Форматування через Prettier
<leader>cke          # Перемикання ESLint (заглушка)
```

#### TypeScript специфічні
```bash
<leader>ca           # Code Actions
<leader>cr           # Перейменування
K                    # Hover інформація з типами
```

## Система лінтерів і форматерів

### ⚠️ Важливо: Мінімальний автозапуск

Ця конфігурація використовує **мінімальний автозапуск** лінтерів для збереження продуктивності. Більшість інструментів запускаються **вручну** через хоткеї.

#### Автоматичні дії
- Форматування при збереженні: `F2` або `:w`
- Очищення пробілів в кінці рядків: автоматично при збереженні
- LSP діагностика: в реальному часі

#### Ручний запуск лінтерів
```bash
<leader>ckc          # Перемикання Codespell (перевірка орфографії)
<leader>ckd          # Перемикання djlint (Django шаблони)
<leader>cke          # Перемикання ESLint (JavaScript)
<leader>ckf          # Перемикання Flake8 (Python)
```

## Управління файлами та проектами

### Файловий провідник (F9)
```bash
F9 або <leader>ee    # Відкрити файловий провідник

# В провіднику:
Enter                # Відкрити файл/папку
a                    # Створити файл/папку
d                    # Видалити
rn                   # Перейменувати
c                    # Копіювати
x                    # Вирізати
p                    # Вставити
f                    # Фільтрувати файли
F                    # Очистити фільтр
H                    # Показати/сховати приховані файли
r                    # Оновити
q або Esc            # Закрити
```

### Пошук файлів і тексту
```bash
<leader>ff           # Знайти файли
<leader>fg           # Пошук по тексту (Live Grep)
<leader>fG           # Пошук включаючи ігноровані файли
<leader>fb           # Знайти буфери
<leader>fo           # Останні файли
<leader>fd           # Символи документу
```

### Заміна тексту
```bash
<leader>fc           # Знайти і замінити через Telescope
<leader>fC           # Знайти і замінити (включно з ігнорованими)
<leader>fx           # Замінити поточне слово

# В поточному файлі
:%s/old/new/g        # Замінити все
:%s/old/new/gc       # Замінити з підтвердженням
```

## Робота з темами

### Тимчасова зміна теми
```bash
<leader>ut           # Відкрити перемикач тем
# Стрілки для навігації, Enter для застосування
# Тема діє тільки до перезапуску
```

### Постійна зміна теми
```bash
<leader>us           # Встановити постійну тему
# Вибрати тему та натиснути Enter
# Тема збережеться назавжди

<leader>ui           # Інформація про поточну тему
```

## Управління табами

### Розумне закриття табів
```bash
<leader>tq           # Розумне закриття (повертає до Dashboard якщо останній)
<leader>tc           # Закрити всі збережені таби
<leader>tQ           # Примусове закриття без збереження
<leader>tA           # Закрити все і вийти
ZZ                   # Зберегти і розумно закрити таб
```

### Створення і навігація
```bash
<leader>tn           # Новий таб
Ctrl+t               # Альтернативний спосіб створити таб
<leader>tO           # Закрити інші таби
```

## Термінали та інструменти

### Вбудований термінал
```bash
<leader>xtf          # Плаваючий термінал
<leader>xth          # Горизонтальний термінал
<leader>xtv          # Вертикальний термінал
Ctrl+\               # Швидкий доступ до терміналу
```

### Спеціалізовані терміналы
```bash
<leader>xtp          # Python REPL з venv
<leader>xtn          # Node.js REPL
```

## Git інтеграція

### Операції з Git hunks
```bash
<leader>gs           # Stage hunk
<leader>gr           # Reset hunk
<leader>gp           # Попередній перегляд hunk
<leader>gb           # Blame рядка
<leader>gd           # Diff поточного файлу
<leader>gt           # Перемкнути blame
```

### Навігація по Git змінам
```bash
]c                   # Наступний Git hunk
[c                   # Попередній Git hunk
```

## Розширені можливості

### Код аналіз і навігація
```bash
F7                   # Code Inspector (всі символи файлу)
<leader>cs           # Символи документу
<leader>cg           # Символи документу (згруповані)
<leader>cw           # Символи робочого простору
<leader>af           # Aerial символи
```

### TODO коментарі
```bash
<leader>st           # Знайти TODO коментарі
<leader>sT           # Знайти TODO/FIX/FIXME
]t                   # Наступний TODO
[t                   # Попередній TODO
```

### Форматування документів
```bash
<leader>df           # Форматувати документ (мова-специфічно)
<leader>dr           # Перемкнути рендеринг (Markdown/RST)

# Налаштування відступів
<leader>ds2          # 2 пробіли
<leader>ds4          # 4 пробіли
<leader>dst          # Перемкнути таби ↔ пробіли

# ColorColumn
<leader>dc0          # Перемкнути ColorColumn
<leader>dc1          # ColorColumn на 79 символах
<leader>dc2          # ColorColumn на 120 символах
```

### Буфер обміну
```bash
<leader>ya           # Копіювати весь файл
<leader>yy           # Копіювати виділення
<leader>yp           # Вставити з системного буферу
```

## Налаштування та система

### Конфігурація
```bash
<leader>xr           # Перезавантажити конфігурацію
<leader>xm           # Mason (управління LSP серверами)
<leader>xu           # Undotree (історія змін)
<leader>xf           # Перемкнути автоформатування при збереженні
```

### Діагностика та помилки
```bash
]d                   # Наступна діагностика
[d                   # Попередня діагностика
<leader>cc           # Діагностика рядка
<leader>cC           # Діагностика проекту
<leader>cl           # Список діагностики
<leader>cq           # Quickfix список
```

## Структура конфігурації

```
~/.config/nvim/
├── lua/
│   ├── config/
│   │   ├── keymaps.lua        # Всі базові хоткеї
│   │   ├── safe-save.lua      # Безпечне збереження файлів
│   │   └── langmap-helper.lua # Підтримка української розкладки
│   └── plugins/
│       ├── legendary.lua      # Централізована система команд
│       ├── additional.lua     # Основні плагіни
│       ├── lsp.lua           # Language servers
│       ├── mason.lua         # LSP installer
│       ├── formatting.lua    # Форматування і лінтінг
│       ├── nvim-tree.lua     # Файловий провідник
│       ├── telescope.lua     # Пошук і навігація
│       ├── theme.lua         # Система тем
│       └── dashboard.lua     # Стартовий екран
└── .last_colorscheme         # Збережена тема
```

## Швидкий старт

### 1. Відкрити файловий провідник
```bash
F9                   # Відкрити файловий менеджер
<leader>ee           # Альтернативний спосіб
```

### 2. Навігація між табами
```bash
Alt + Left / F5      # Попередній таб
Alt + Right / F6     # Наступний таб
Alt + 1-9            # Перейти до табу номер 1-9
```

### 3. Пошук і заміна
```bash
<leader>fg           # Пошук тексту у всіх файлах
:%s/old/new/g        # Заміна в поточному файлі
```

### 4. Навігація по коду
```bash
gd                   # Перейти до визначення
gr                   # Показати посилання
F7                   # Переглянути символи документу
<leader>ca           # Code actions
```

### 5. Python розробка
```bash
<leader>cvc          # Вибрати віртуальне середовище
<leader>xtp          # Python термінал з venv
<leader>df           # Форматування (isort + black)
F2                   # Зберегти і форматувати
```

## Поради та найкращі практики

1. **Освойте систему табів** - дозвольте файлам відкриватися в табах природно
2. **Використовуйте Legendary** - натисніть `<Space>` і досліджуйте доступні опції
3. **Освойте F-клавіші** - F7-F10 надають миттєвий доступ до панелей
4. **Використовуйте LSP навігацію** - використовуйте `gd`, `gr`, `gi` для дослідження коду
5. **Експериментуйте з темами** - використовуйте `<leader>ut` для знаходження своїх уподобань
6. **Налаштовуйте лінтери поступово** - вмикайте інструменти за потребою для проектів
7. **Використовуйте модальний провідник файлів** - `F9` для швидких операцій, `Esc` для закриття
8. **Поєднуйте інструменти пошуку** - використовуйте Telescope + quickfix для масових замін
9. **Використовуйте інтеграцію терміналу** - `Ctrl+\` для швидкого доступу
10. **Тримайте робочий простір чистим** - використовуйте `<leader>tq` для розумного управління табами

Ця конфігурація перетворює Neovim на потужне, сучасне IDE, зберігаючи ефективність та гнучкість, які роблять Vim винятковим. Підхід, орієнтований на таби, заповнює прогалину між традиційними процесами Vim та сучасними очікуваннями IDE.